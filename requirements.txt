# Base Python dependencies for the CUDA 12 fleet env.
# Install torch/torchvision/torchaudio first, then install this file,
# then install vllm last because it rewrites the torch runtime.
ninja==1.13.0
pydantic==2.13.4
pyyaml==6.0.3
huggingface_hub==1.23.0
transformers==5.14.1
deepspeed==0.19.2
datasets==5.0.0
ray==2.56.0
mlflow==3.14.0
wandb==0.28.1
peft==0.19.1
math-verify[antlr4-13-2]==0.9.0
